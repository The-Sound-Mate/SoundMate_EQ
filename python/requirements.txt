# SoundMate EQ — runtime dependencies (pip-compatible mirror of pyproject.toml)
#
# Install with:
#     pip install -r requirements.txt
#
# Or, if you use uv:
#     uv sync                       # reads pyproject.toml (preferred)
#
# essentia is *optional* — install separately with `pip install essentia`
# on Linux / macOS.  Windows has no official essentia wheel; the tempo
# extractor falls back to librosa automatically.

numpy>=1.26
scipy>=1.13
soundfile>=0.12
librosa>=0.10
pyloudnorm>=0.1.1
pyyaml>=6.0
rich>=13.7
