# Claude 개발 요청용: SoundMate 오디오 엔진 아키텍처 및 로직 가이드

본 문서는 코드를 직접 짜기 전, 클로드(Claude)가 **Windows 오디오 서비스 아키텍처와 SoundMate의 구동 원리**를 완벽히 이해하도록 로직 위주로 정리한 지침입니다.

---

## 1. 오디오 프레임 처리 로직 (Audio Processing Flow)

오디오 엔진(`SoundMate_APO.dll`)이 `audiodg.exe` 내에서 실시간으로 데이터를 처리하는 흐름입니다.

1.  **입력 단계**: Windows 오디오 서비스로부터 원본 PCM 데이터 버퍼를 수신합니다.
2.  **안전 검사 (Fail-Safe Check)**:
    -   엔진 활성화 상태 및 필터 유효성을 즉시 검사합니다.
    -   만약 이전 프레임에서 연산 지연(Timeout)이나 예외가 발생했다면, 이번 프레임은 연산을 생략하고 즉시 3번으로 점프합니다.
3.  **필터 연산 (Filter Logic)**:
    -   **Biquad Filter Pipeline**: Equalizer APO의 로직을 따라 10개의 독립된 IIR 필터를 순차적으로 통과시킵니다.
    -   **Overflow Guard**: 연산 결과값이 오디오 클리핑(Clipping)을 유발하지 않도록 리미터(Limiter) 로직을 적용합니다.
4.  **예외 처리 및 바이패스 (Passthrough)**:
    -   연산 중 0으로 나누기, 메모리 참조 오류 등이 발생하면 `memcpy`를 통해 원본 데이터를 출력 버퍼로 즉시 복사합니다. 이 로직이 **유튜브 동영상 멈춤 현상을 방지하는 핵심**입니다.

---

## 2. 실시간 설정 동기화 로직 (Atomic Config Swap)

오디오 서비스 재시작 없이 EQ를 반영하는 비동기 통신 로직입니다.

1.  **데이터 전달**: UI/Controller에서 변경된 EQ 값을 **명명된 공유 메모리(Named Shared Memory)**에 기록합니다.
2.  **신호 발생 (Signaling)**: 이벤트 오브젝트(`Event Object`)를 통해 APO DLL에 설정이 변경되었음을 알립니다.
3.  **원자적 교체 (Atomic Swap)**:
    -   APO는 현재 사용 중인 필터 객체를 직접 수정하지 않습니다.
    -   새로운 설정이 담긴 **객체를 별도로 생성**한 후, 오디오 연산 루프 직전에 포인터만 '원자적'으로 교체합니다.
    -   이후 기존 객체는 오디오 연산이 없는 안전한 타이밍에 해제하여 **지직거리는 소음(Glitch)을 차단**합니다.

---

## 3. 설치 및 레지스트리 주입 로직 (Safe Injection)

시스템 오디오를 망가뜨리지 않고 주입되는 단계별 로직입니다.

1.  **기존 APO 탐색**: 현재 장치에 설정된 기본 APO(OEM 드라이버)의 CLSID를 읽어옵니다.
2.  **체이닝 (Chaining) 구성**:
    -   SoundMate를 메인 APO로 등록하되, 기존 APO의 CLSID를 SoundMate의 **'Child APO'** 필드에 저장합니다.
    -   SoundMate 엔진은 자신의 처리가 끝나면 자동으로 Child APO를 호출하여 드라이버 고유 기능(DTS, Dolby 등)이 유지되도록 합니다.
3.  **권한 수리 (Permission Healing)**:
    -   레지스트리 쓰기 전, 현재 프로세스의 토큰에 `SeTakeOwnershipPrivilege`를 활성화합니다.
    -   대상 키의 소유자를 `SYSTEM`으로 강제 변경하고, `Full Control` 권한을 부여한 뒤 작업을 수행합니다. 작업 종료 후에는 원래 권한을 최대한 복구합니다.

---

## 4. 재난 복구 로직 (Nuclear Reset Logic)

오디오 서비스가 시작되지 않거나 소리가 안 날 때의 강제 복구 단계입니다.

1.  **서비스 중단**: `Audiosrv`와 `AudioEndpointBuilder`를 강제 종료하여 레지스트리 락(Lock)을 해제합니다.
2.  **엔드포인트 퍼지 (Endpoint Purge)**:
    -   문제가 발생한 오디오 장치의 소프트웨어 엔드포인트 키를 레지스트리에서 완전히 삭제합니다.
    -   이는 윈도우 오디오 캐시를 비우는 것과 같으며, **'소리 없음/X 표시' 문제를 해결하는 가장 확실한 방법**입니다.
3.  **시스템 재스캔**: `PnPUtil` 또는 `SetupAPI`를 통해 하드웨어 변경 사항을 다시 검사하여 윈도우가 드라이버를 다시 로드하게 유도합니다.
4.  **ACL 하드닝**: 서비스 계정(`LocalService`)이 레지스트리에 접근할 수 있도록 기본 권한(ACL)을 표준값으로 강제 재설정합니다.

---